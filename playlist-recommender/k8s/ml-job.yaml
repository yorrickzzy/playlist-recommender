apiVersion: batch/v1
kind: Job
metadata:
  name: playlist-ml-v2  
spec:
  template:
    metadata:
      labels:
        app: playlist-ml
    spec:
      restartPolicy: Never
      containers:
      - name: playlist-ml
        image: zzy0110/playlist-ml:latest
        command: ["python", "/app/train_model.py"]
        env:
        - name: DATASET_PATH
          value: "/home/datasets/spotify/2023_spotify_ds2.csv"
        volumeMounts:
        - name: spotify-dataset
          mountPath: /home/datasets/spotify  # 这里是 PVC 挂载点
      volumes:
      - name: spotify-dataset
        persistentVolumeClaim:
          claimName: model-pvc  # 绑定到 PVC，而不是 hostPath


